export enum DatabaseProvider {
  TYPEORM = 'typeorm',
  PRISMA = 'prisma',
  MONGOOSE = 'mongoose',
  SEQUELIZE = 'sequelize',
}
export enum RelationalDatabaseType {
  POSTGRES = 'postgres',
  MYSQL = 'mysql',
  SQLITE = 'sqlite',
  MSSQL = 'mssql',
  ORACLE = 'oracle',
}
